# LoopScope Phase 7 Control

```text
CONTROL_ID=LOOPSCOPE_PHASE7_CONTROL_V1
STATUS=PHASE7_TERMINAL_PASS_AFTER_GATE_F
ACTIVATION=AUTHORIZED_2026-08-15
PHASE=LoopScope Phase 7
HYPOTHESIS=H7_BASE_ARCHITECTURE_TRAJECTORY_V3_AND_FIXED_OUTCOME_V2
PLANNING_THREAD=01a0013e-71c3-7c90-a547-4059b462dc7e
PLANNING_THREAD_TITLE=plan-audit-LoopScope-Base架构泛化轨迹与V3选窗-第7阶段
ACTIVE_GATE=NONE
AUTHORIZED_EXECUTOR=NONE
AUTHORIZED_EXECUTOR_TITLE=NONE
LAST_GATE_F_EXECUTOR=01a00965-fa7f-7411-b060-0c8315a94711
LAST_GATE_F_EXECUTOR_TITLE=execute-LoopScope-V3-Top2-Outcome-第7阶段-Gate F
LAST_GATE_E_EXECUTOR=01a00569-e66d-7eb2-9499-56b01d268366
LAST_GATE_E_EXECUTOR_TITLE=execute-LoopScope-TopAvailable-Outcome-第7阶段-Gate E
LAST_GATE_D_EXECUTOR=01a00522-11a9-70d1-80e4-5d4401deb68c
LAST_GATE_D_EXECUTOR_TITLE=execute-LoopScope-V3-Scoring-第7阶段-Gate D
LAST_GATE_C_EXECUTOR=01a0036f-fc1d-7412-b043-b93b5f088398
LAST_GATE_C_EXECUTOR_TITLE=execute-LoopScope-Full-Acquisition-第7阶段-Gate C
SUPERSEDED_GATE_C_EXECUTOR=01a00215-a3a1-7841-9079-e87cf78571e0
SUPERSEDED_GATE_C_EXECUTOR_TITLE=execute-LoopScope-Full-Acquisition-第7阶段-Gate C
SUPERSEDED_GATE_C_REASON=ABNORMAL_TERMINATION_WITHOUT_REQUIRED_PLANNING_TERMINAL_DELIVERY_RETAINED_READ_ONLY
LAST_GATE_B_EXECUTOR=01a001c1-3ff9-7370-abbb-b32128897a08
LAST_GATE_B_EXECUTOR_TITLE=execute-LoopScope-Debug-Smoke-第7阶段-Gate B
LAST_GATE_A_EXECUTOR=01a00159-6c55-72b3-bbf8-5ac798868258
LAST_GATE_A_EXECUTOR_TITLE=execute-LoopScope-三模型Provenance与轨迹探测实现-第7阶段-Gate A
STANDING_PHASE_CONTINUATION=NONE_PHASE_TERMINAL
OPERATIONAL_PERMISSION_DECISIONS=DELEGATED_TO_PLANNING_FOR_SMALLEST_SUFFICIENT_GATE_SCOPED_OPERATIONS
DEFAULT_EXECUTOR_CONFIGURATION=INHERIT_USER_CONFIG_NO_OVERRIDE
TERMINAL_EXECUTOR_GATE=NONE
PHASE_TERMINAL_OWNER=PLANNING_THREAD
CURRENT_DECISION=GATE_F_PASS_PHASE7_RECLOSED
GATE_A_STATE=PASS
BLOCKER=NONE
REQUIRED_USER_DECISION=NONE
ACTIVE_HANDOFF=NONE
ACTIVE_SUPPLEMENT=NONE
ACTIVE_SCIENTIFIC_AMENDMENT=.planning/phase7/loopscope_phase7_gate_f_v3_top2_amendment_20260816.md
FORMAL_EXPANSION_CONDITION=NONE_PHASE_TERMINAL
HISTORICAL_SUPPLEMENTS=.planning/phase7/loopscope_phase7_gate_a_repair_and_permission_supplement_2.md;.planning/phase7/loopscope_phase7_gate_a_download_route_supplement_3.md;.planning/phase7/loopscope_phase7_gate_a_access_resume_supplement_4.md;.planning/phase7/loopscope_phase7_gate_a_token_mirror_supplement_5.md;.planning/phase7/loopscope_phase7_gate_a_gemma_access_recheck_supplement_6.md;.planning/phase7/loopscope_phase7_gate_c_resource_utilization_supplement_3.md;.planning/phase7/loopscope_phase7_gate_c_same_gpu_multi_shard_supplement_4.md;.planning/phase7/loopscope_phase7_gate_c_direct_a800_supplement_5.md;.planning/phase7/loopscope_phase7_gate_c_cpu_per_gpu_supplement_6.md;.planning/phase7/loopscope_phase7_gate_e_gpu_utilization_supplement_1.md;.planning/phase7/loopscope_phase7_gate_e_resource_recovery_supplement_2.md;.planning/phase7/loopscope_phase7_gate_e_gemma_two_a800_supplement_3.md;.planning/phase7/loopscope_phase7_gate_e_gemma_batch_recovery_supplement_4.md
GATE_B_STATE=PASS
GATE_C_STATE=PASS
GATE_D_STATE=PASS
PLANNING_PLOTTING_STATE=PASS
GATE_E_STATE=PASS
GATE_F_STATE=PASS
PLANNING_CLOSEOUT_STATE=PASS_PHASE7_TERMINAL
AUDITED_GATE_E_COMMIT=65b39762caed6ab2443d03314d29c58d73d20f3d
AUDITED_GATE_E_RUN_ROOT=/hpc2hdd/home/xhuang225/workspaces/training_free_looped_transformers_loopscope/runs/phase7-gate-e-20260816T041724Z-formal-remaining24-gitless
AUDITED_GATE_E_ANALYSIS=/hpc2hdd/home/xhuang225/workspaces/training_free_looped_transformers_loopscope/runs/phase7-gate-e-20260816T041724Z-formal-remaining24-gitless/analysis/combined_analysis.json
AUDITED_GATE_F_COMMIT=08da7733a1e8402677184c860d0ec35f041335aa
AUDITED_GATE_F_RUN_ROOT=/hpc2hdd/home/xhuang225/workspaces/training_free_looped_transformers_loopscope/runs/phase7-gate-f-20260816T073546Z-formal
AUDITED_GATE_F_ANALYSIS=/hpc2hdd/home/xhuang225/workspaces/training_free_looped_transformers_loopscope/runs/phase7-gate-f-20260816T073546Z-formal/analysis/combined_analysis.json
GATE_F_HISTORICAL_SUPPLEMENTS=.planning/phase7/loopscope_phase7_gate_f_git_push_permission_supplement_1.md;.planning/phase7/loopscope_phase7_gate_f_runtime_path_supplement_2.md;.planning/phase7/loopscope_phase7_gate_f_analysis_lifecycle_supplement_3.md;.planning/phase7/loopscope_phase7_gate_f_fresh_verifier_lifecycle_supplement_4.md
PHASE_AUDIT=.planning/phase7/loopscope_phase7_gate_f_and_phase_reclose_audit_pass_20260816.md
PRIOR_PHASE_TERMINAL=PHASE7_TERMINAL_PASS_AFTER_GATE_E_SUPERSEDED_BY_USER_AUTHORIZED_GATE_F_EXTENSION_20260816
```

## 1. 当前权威边界

- 本文件只记录第七阶段动态授权与 Gate 状态；Gate A–F 已由 planning 判定 `PASS`
  并撤销对应 executor 权限。原 Gate C executor `01a00215-a3a1-7841-9079-e87cf78571e0` 因未按协议向
  planning 投递终态而被撤权；replacement Gate C executor
  `01a0036f-fc1d-7412-b043-b93b5f088398` 已正常终态并同样撤权，二者均按项目政策保留为可见只读
  provenance。Gate D executor `01a00522-11a9-70d1-80e4-5d4401deb68c` 与 Gate E executor
  `01a00569-e66d-7eb2-9499-56b01d268366` 均已正常终态并撤权。用户随后明确追加
  Gate F，因此 Gate E 后的 `PHASE7_TERMINAL_PASS` 保留为前一截止点。Gate F executor
  `01a00965-fa7f-7411-b060-0c8315a94711`（`execute-LoopScope-V3-Top2-Outcome-第7阶段-Gate F`）
  已正常投递唯一终态并被撤权；planning 已完成 recclose。当前无 authorized executor、无 active Gate，
  Phase 7 重新终态；不创建 Gate G。
- 稳定科学合同以
  `.planning/phase7/loopscope_phase7_scientific_contract.md` 为准。
- Gate 总体路径以 `.planning/phase7/loopscope_phase7_plan.md` 为准。
- 跨阶段选窗数学以
  `.planning/selector_rules/aggregate_common_turn_v3_absolute_rate.md` 中的
  `AGGREGATE_COMMON_TURN_V3_ABSOLUTE_RATE@3.1.0` 为准；该规则本身不授予执行权限。
- 第六阶段已经终态，不能作为第七阶段执行授权。Phase 6 的 outcome 不得进入 Phase 7
  trajectory、selector、绘图标注或 Gate 决策。

## 2. 第七阶段唯一新采集矩阵

```text
Qwen/Qwen2.5-3B
meta-llama/Llama-3.2-3B
google/gemma-2-2b
```

`Qwen/Qwen3-1.7B-Base` 与 `Qwen/Qwen3-4B-Base` 仅为历史背景；第七阶段不重新采集、
不重新评分、不重绘，也不把它们加入三模型正式 membership。

## 3. 已确认的核心配置

```text
task=cais/mmlu
split=validation
population=1531 identities / 57 subjects
num_fewshot=5
fewshot_split=dev
prompt=plain non-chat standard MMLU
probe=rendered prefix final non-padding token ending the current query's Answer:
forward=one native zero-loop use_cache=false forward per identity
selector=AGGREGATE_COMMON_TURN_V3_ABSOLUTE_RATE@3.1.0
candidate_depth_rule=trim_per_side(L)=ceil(0.30*L), central containment, widths 3/4/5/6
bootstrap=within-category equal-category-macro joint bootstrap, R=2000, seed=20260801
outcome_access=FORBIDDEN
loop_execution=FORBIDDEN
test_split=FORBIDDEN
```

上述 outcome/test 禁令适用于 Gate A–D 与 planning plotting checkpoint。用户于 2026-08-15 追加的
Gate E recipe 由 `.planning/phase7/loopscope_phase7_gate_e_outcome_amendment_20260815.md` 冻结：

```text
dataset=cais/mmlu
split=test
population=14042 identities / 57 subjects
num_fewshot=5
per_model_panel=1 no-loop + authorized width-4 V3 score top-available windows * k{2,3} * cache{first,last} * mode=block
cells_per_model=13,13,9
total_cells=35
ABSTAIN_policy=Gemma uses its two finite width-4 scores; exploratory post-ranking outcome
```

模型的 exact resolved revision、decoder-layer count、final-norm/lm-head 路径和四个 continuation
surface 的 tokenizer 闭合已由 Gate A 直接读取并冻结；当前权威值见 admission sidecar 与第 15 节。

## 4. Gate 状态表

| Gate | 目标 | Executor | 状态 | 下一 admission |
| --- | --- | --- | --- | --- |
| A | 三模型 provenance、合同、最小多架构 producer/verifier 与 targeted tests | `01a00159-6c55-72b3-bbf8-5ac798868258` | `PASS` | 已满足 |
| B | 三模型四 identity 的 `debug` GPU smoke | `01a001c1-3ff9-7370-abbb-b32128897a08` (`execute-LoopScope-Debug-Smoke-第7阶段-Gate B`) | `PASS` | 已满足 |
| C | 三模型 validation-1531 正式逐层 trajectory acquisition 与 data seal | `01a0036f-fc1d-7412-b043-b93b5f088398` (`execute-LoopScope-Full-Acquisition-第7阶段-Gate C`) | `PASS` | 已满足 |
| D | 三模型 CPU-only V3.1 评分、终态与独立 verifier | `01a00522-11a9-70d1-80e4-5d4401deb68c` (`execute-LoopScope-V3-Scoring-第7阶段-Gate D`) | `PASS` | 已满足 |
| Planning plotting checkpoint（非 Gate） | 只读 Gate D source-data 完成绘图、QA、outcome-blind 报告与 Gate E panel freeze | planning thread `01a0013e-71c3-7c90-a547-4059b462dc7e` | `PASS_FIGURES_AND_QA` | 图表与报告已完成；用户已批准 3/3/2 panel amendment |
| E | 三模型 MMLU test-14042、13/13/9-cell Top-Available Width-4 full outcome | `01a00569-e66d-7eb2-9499-56b01d268366` (`execute-LoopScope-TopAvailable-Outcome-第7阶段-Gate E`) | `PASS` | 已满足 |
| Gate F | 三模型 V3 point-rank top-2，24 个新 loop cells 全量 outcome | `01a00965-fa7f-7411-b060-0c8315a94711` (`execute-LoopScope-V3-Top2-Outcome-第7阶段-Gate F`) | `PASS` | 24/24 + 3 baseline references、analysis、fresh verifier 已闭合 |
| Planning recclose（非 Gate） | 更新 Gate F outcome 报告并重新终结 Phase 7 | planning thread `01a0013e-71c3-7c90-a547-4059b462dc7e` | `PASS_PHASE7_TERMINAL` | 已完成；不创建 Gate G |

状态名称本身不授权执行。Gate A–E 各自必须使用一个新的独立、用户可见 executor；规划线程
及其 subagent 都不能替代这些实验/分析 executor。Gate D `PASS` 后先由 planning 完成绘图与
panel freeze，再创建独立 Gate E executor；Gate E `PASS` 后的报告更新与阶段终审由 planning
thread 亲自完成，不创建 Gate F。

## 5. 当前本地状态与保护

- Git 专用 clone 为 `loopscope-tflt/`，当前 `loopscope` worktree 已存在用户/前序任务的
  未提交修改。Planning 激活时只在 `AGENTS.md` 追加了 Phase 7 稳定政策；其余前序 dirty
  files 保持原样。
- Phase 7 激活时已重新读取 exact dirty state 并冻结下述保护清单；不得 reset、stash、
  覆盖或把它们混入 Phase 7 commit。
- Planning-owned 控制与报告修改位于 `.planning/phase7/`、`资产/报告/phase7/` 及其外层索引，
  不由 Gate A executor 提交。
- `loopscope-tflt/AGENTS.md` 已由 planning thread 追加 Phase 7 稳定条款；该文件仍含前序
  未提交政策修改，Gate A executor 不得暂存、提交、回滚、覆盖或进一步修改它。
- 激活时 Git 基线为 branch `loopscope`、commit
  `37241f6fb52aa6ee89a5b4e24ac607f7312e77ac`。以下 dirty paths 全部视为受保护的前序状态：
  `AGENTS.md`、`docs/loopscope_phase2.md`、`docs/loopscope_phase4.md`、
  `docs/loopscope_phase5.md`、`docs/loopscope_phase6.md`、
  `scripts/loopscope/run_qwen4_phase4_p4b.py`、`src/tflt/loopscope/phase4_outcome.py`。
  Gate A 只能显式暂存 handoff 授权的新 Phase 7 路径及确有必要的未脏最小注册文件。

## 6. 激活条件与当前授权

只有同时满足以下条件，planning 才能授权 Gate A：

1. 用户明确说“启动/执行第七阶段”或等价指令；认可本计划本身不算执行授权。
2. 重新读取本 control、科学合同、总体计划、`loopscope-tflt/AGENTS.md` 和 live Git 状态。
3. 冻结 exact Gate A base、允许写入路径、HPC2 读取/网络权限以及已有 dirty state 的保护方式。
4. 创建一个新的独立 Gate A executor，标题为
   `execute-LoopScope-三模型Provenance与轨迹探测实现-第7阶段-Gate A`。
5. 写入 `.planning/phase7/loopscope_phase7_gate_a_handoff.md`，绑定 exact executor，并取得
   可见投递确认。
6. Gate B–D 与 planning closeout 继续保持锁定。

上述激活条件已于 2026-08-15 满足。用户已授权本 planning thread 按 Gate A→B→C→D
持续推进，并在 Gate D `PASS` 后亲自完成 planning closeout。Gate A 的 HPC2 权限只包括
项目相关代码、配置、环境与已有 tokenizer/model cache provenance 的有界只读检查，以及
不加载模型权重的 CPU tokenizer/config admission；不授权远端写入、模型 forward、GPU、
Slurm、数据 acquisition 或 selector execution。

用户已把第七阶段普通 operational permission 的代决权委托给本 planning thread。若当前
executor 因缺少项目路径访问、pinned 下载/环境维护、远端调试、具备最小复现支持的必要实现
路径修改、GPU/Slurm、job control 或 write-once fresh retry 权限而无法推进，planning 直接
核实材料性并向同一 Gate executor 授予最小充分权限，更新 control/supplement 并确认投递；
不要求用户逐项批准。该委托不覆盖科学合同、信息提前解封、阶段/矩阵扩张、不可逆破坏、
凭证/秘密、外部人员或法律承诺。

后续 Gate 如需 HPC 计算资源，由 planning 在各 Gate handoff 内依
`research-gate-orchestrator` 冻结具体 envelope：debug/smoke 一律使用 `debug` partition 且
目标运行时间 `<30min`；正式长任务必须先有同 decision-critical path 的有效 preflight，
随后使用普通用户合法可用的最高优先级 partition/QoS，并结合显存适配、smoke 实测 runtime、
当前可用性、队列估计与 time-to-result 选择 GPU。该代决不授权 privileged QoS、改变科学
合同、提前读取 outcome、跨 Gate 或破坏性操作。

所有 Gate executor 必须在执行前完整重读并遵守 `research-gate-orchestrator`，以最短可信
实验主线、材料性验收和 deletion test 为工作准则。每个 executor 可在能实质缩短当前 Gate
时使用 subagent，任一时刻最多三个；subagent 只继承明确委派的当前 Gate 窄任务，不能跨
Gate、改科学合同、独立执行 Git integration/GPU/Slurm、发送 terminal event 或替代 executor
责任。

## 7. Gate A 第一次终审与返修授权

2026-08-15，planning 收到 exact executor
`01a00159-6c55-72b3-bbf8-5ac798868258` 的 `GATE_A_FINAL_AUDIT`。轻量验收确认：Phase 7
新增文件仍在授权路径内，七个 protected dirty paths 未被暂存或覆盖，targeted 12 tests 通过；
但三个 exact target snapshots 均不在已审计 HPC2 cache，因此 live revision、真实 `L` 与 choice
surface 尚未闭合。该缺口属于原 handoff 禁止 metadata/tokenizer 下载造成的 ordinary
operational blocker，可由 planning 的既有代决权消除。

Planning 同时抽查出 Gate B 正常路径上的三个材料缺陷：launcher 使用
`AutoModelForCausalLM`，但 producer 依赖通常不存在的 `last_hidden_state`；launcher 未把模型
与输入显式放到 CUDA；`--cache-dir` 未传给 `from_pretrained`。因此 Gate A 决定为
`PASS_WITH_FIXES`，由同一 executor 按
`.planning/phase7/loopscope_phase7_gate_a_repair_and_permission_supplement_2.md` 完成唯一一次捆绑
返修。用户随后授权缺失模型文件优先走 HF 清华镜像；下载顺序和边界由
`.planning/phase7/loopscope_phase7_gate_a_download_route_supplement_3.md` 补充。Gate B–D 与
planning closeout 保持锁定。

## 8. Gate A 返修终审决定

Planning 于 2026-08-15 收到 exact executor 的 `GATE_A_REPAIR_FINAL_AUDIT`，并独立抽查
admission sidecar、三个 normal-path 修复、focused 17 tests、staged membership 与 protected
dirty state。代码返修闭合，Qwen/Qwen2.5-3B metadata/tokenizer admission 通过；但
`meta-llama/Llama-3.2-3B` 与 `google/gemma-2-2b` 在 existing cache、HF mirror 和 official
fallback 路径后仍没有可用 snapshot。官方模型页也分别要求账号持有人同意 Llama 联系信息/
协议以及 Gemma 使用许可。

这不是 planning 已获 ordinary operational-permission 代决权能覆盖的工程权限；license
acceptance、credential 与账号访问必须由用户本人决定。Gate A 因此为 `BLOCK_MODEL_ACCESS`，
executor 活动权限撤销，Gate B–D 继续锁定。完整决定见
`.planning/phase7/loopscope_phase7_gate_a_audit_decision_block_20260815.md`。若用户完成两个 exact
repo 的 HF access 并确认 HPC2 已有非交互认证可用，planning 可重新授权同一个 Gate A executor
只补齐剩余 admission；不得在未确认前重试或改模型。

## 9. 用户完成许可后的 Gate A 恢复

用户于 2026-08-15 确认 Gemma 已授权，并在已登录的 Hugging Face 浏览器会话中直接打开
`meta-llama/Llama-3.2-3B/blob/main/config.json` 看到 JSON 内容，满足账号持有人许可完成条件。
Planning 因此按 standing phase continuation 恢复同一个 Gate A executor，仅授权
`.planning/phase7/loopscope_phase7_gate_a_access_resume_supplement_4.md` 中的 HPC2 existing-auth
metadata/tokenizer retry。该恢复不授权 token 读取/修改、login、权重、forward、GPU/Slurm、数据
或 Gate B；若 HPC2 现有认证对应不同账号，executor 只返回窄 blocker。

## 10. Gate A access-resume 终审决定

Planning 于 2026-08-15 收到 exact executor 的 `GATE_A_ACCESS_RESUME_FINAL_AUDIT`。轻量验收确认
本地代码、Git 与七个 protected dirty paths 均未变化，Qwen admission 保持既有闭合，且没有发生
权重、forward、GPU/Slurm、MMLU 数据或凭证动作。Llama 与 Gemma 的镜像 metadata 请求仍返回
gated 403；official Hub 正常尝试与唯一一次 fresh retry 均在 TLS/repo-info 阶段被连接重置，因而
executor 无法判定 HPC2 既有认证是否属于刚完成许可的账号。

用户随后已在浏览器端创建新的 read token，但尚未在 HPC2 交互式登记。凭证输入必须由用户本人在
远端终端完成，不能由 planning 或 executor 读取、代填或经聊天传递。Gate A 因此保持 `BLOCK`，
executor 活动权限撤销；Gate B–D 继续锁定。用户完成 HPC2 `hf auth login` 后，planning 才会向
同一个 Gate A executor 下发新的最小补充授权，限定为认证可达性验证与两模型 metadata/tokenizer
admission，不授权权重、forward、GPU/Slurm、数据或 Gate B。完整决定见
`.planning/phase7/loopscope_phase7_gate_a_access_resume_audit_decision_block_20260815.md`。

## 11. HPC2 交互式登录失败后的最小恢复路径

用户按要求在 HPC2 亲自运行 `hf auth login`，CLI 在保存 token 前调用 official Hub `whoami`；该
请求在 TLS handshake 阶段再次以 `ConnectionResetError(104)` 失败。用户提供的 traceback 未包含
token 内容，且错误发生在远端网络校验层，没有证据表明 token 无效。Planning 的无凭证检查确认
本地到 official Hub 可达，但 HPC2 direct official path 不可达；短时 SSH reverse-SOCKS 诊断也未
形成可供该 Python 环境使用的稳定通路，因此不继续扩展代理工程。

远端 `huggingface_hub` 已确认支持 `HF_TOKEN_PATH`。为避免覆盖现有默认凭证，下一用户动作改为在
HPC2 交互式 shell 中把新建 read token 写入独立的
`/hpc2hdd/home/xhuang225/.cache/huggingface/phase7_read_token`，权限必须为 `600`；token 仍不得经聊天
传输。用户只需确认该文件非空且 mode=600，不提供内容。随后 planning 可恢复同一 Gate A executor，
限定其以 `HF_TOKEN_PATH` 和既有 `hf-mirror.com` 路由完成 Llama/Gemma metadata/tokenizer admission；
执行线程不得读取、打印、复制或改写 token，official fallback 在 connectivity 修复前不作为必需路径。

## 12. Phase 7 专用 read token 后的 Gate A 恢复

用户于 2026-08-15 在 HPC2 交互式 shell 中创建
`/hpc2hdd/home/xhuang225/.cache/huggingface/phase7_read_token`。Planning 随后只读确认该文件非空、
owner 为 `xhuang225`、mode 为 `600`，未读取或输出内容。该条件消除了“凭证尚未持久化”的用户动作
blocker；official Hub 直连仍不可用，但不影响本次 token-backed mirror 路径。

Planning 依 standing continuation 与 operational-permission delegation 恢复同一个 Gate A executor，
唯一活动授权为 `.planning/phase7/loopscope_phase7_gate_a_token_mirror_supplement_5.md`：通过
`HF_TOKEN_PATH` 和 `HF_ENDPOINT=https://hf-mirror.com` 获取 Llama/Gemma 的 config/tokenizer 轻量
metadata，随后离线闭合 admission。不得读取、打印、复制、修改或删除 token；不得下载权重、运行
forward/GPU/Slurm、读取 MMLU 数据或进入 Gate B。

## 13. Token-backed mirror 终审决定

Planning 于 2026-08-15 收到 exact executor 的 `GATE_A_TOKEN_MIRROR_FINAL_AUDIT`，并轻量核对
`configs/loopscope/phase7_model_admission.json`、Phase 7 runbook 与 live Git/staged state。Qwen 保持
既有 PASS；Llama 通过同一个 Phase 7 read token 和 mirror 成功闭合 revision、`L=28`、四个架构路径、
四 choice surfaces 与 26 个候选窗；Gemma 则由同一路径返回 gated 403，明确说明 access request
`awaiting review`，且 cache 仍无 snapshot 文件。该对照证明 token 文件和 mirror 认证路径正常，剩余
blocker 是 Gemma 仓库方审批状态，不是代码、网络或 token 持久化缺陷。

三模型完整 membership 是 Gate A must-pass，不能在 Gemma 缺失时进入 GPU smoke。Planning 因此判定
`BLOCK_GEMMA_ACCESS_AWAITING_REPO_REVIEW`，撤销 executor 活动权限，Gate B–D 继续锁定。用户需在
`google/gemma-2-2b` 页面确认状态已经从 pending/awaiting review 变为 accepted/可直接读取文件；仅
提交申请或接受条款不等于审批完成。确认 accepted 后，planning 才会恢复同一 Gate A executor，仅对
Gemma 做一次 token-backed mirror metadata admission。完整决定见
`.planning/phase7/loopscope_phase7_gate_a_token_mirror_audit_decision_block_20260815.md`。

## 14. 用户要求执行线程直接复查 Gemma access

用户于 2026-08-15 明确要求由执行线程再次访问尝试。Planning 因此恢复同一个 Gate A executor，
只授权 `.planning/phase7/loopscope_phase7_gate_a_gemma_access_recheck_supplement_6.md` 中的一次 Gemma
token-backed mirror metadata recheck。该动作以真实 repository access 结果为准，不依赖网页状态解释；
不得重复 Qwen/Llama、不得读取或修改 token、不得下载权重、运行 forward/GPU/Slurm、读取 MMLU 数据
或进入 Gate B。成功则离线闭合 Gemma admission 并返回终审；仍为 403 awaiting review 则立即 BLOCK，
不继续重试或扩展网络路径。

## 15. Gate A 终审 PASS 与 Gate B 授权

Planning 于 2026-08-15 收到 exact Gate A executor 的
`GATE_A_GEMMA_RECHECK_FINAL_AUDIT`。最小独立验收直接解析
`loopscope-tflt/configs/loopscope/phase7_model_admission.json`，确认状态为
`PASS_THREE_MODEL_ADMISSION`：Qwen2.5-3B、Llama-3.2-3B、Gemma-2-2B 的 resolved revision、真实
`L=36/28/26`、架构路径、四个单 token 且互异的 choice surfaces 与公式候选域 `42/26/26`
全部闭合。Phase 7 discovery tests 17/17 通过；branch/HEAD/required ancestry、空 staged membership、
七个 protected dirty paths 与 `git diff --check` 均保持合同内。一次非 canonical unittest 调用的
sibling import loader error 已由 canonical discovery 命令通过证明为调用偏差，不影响 admission。

Planning 因此判定 Gate A `PASS`，撤销 executor
`01a00159-6c55-72b3-bbf8-5ac798868258` 的活动权限。完整决定见
`.planning/phase7/loopscope_phase7_gate_a_audit_decision_pass_20260815.md`。

依 standing continuation，Gate B 已绑定新的独立 executor
`01a001c1-3ff9-7370-abbb-b32128897a08`，标题
`execute-LoopScope-Debug-Smoke-第7阶段-Gate B`，唯一授权由
`.planning/phase7/loopscope_phase7_gate_b_handoff.md` 冻结。Gate B 先对已测试但尚未提交的 Phase 7
专用工作集作 focused commit/push 与 HPC2 clean fast-forward，再只在 `debug` partition 运行每模型
one-identity preflight 和 fresh four-identity smoke。Gate C、Gate D、planning plotting checkpoint、
Gate E 与 planning closeout 保持锁定。

## 16. 用户追加 Gate E 全量 Outcome

用户于 2026-08-15 明确将后续 full outcome 保留在 Phase 7，并授权 planning 按现有 event-driven
协议继续至 Gate E 完成。三项 recipe 已直接确认：`cais/mmlu` test 全量 14,042 identities / 57
subjects、标准 5-shot；每模型一个 no-loop baseline，加 Gate D width-4 V3 final-score top-3 的
`k={2,3} × cache={first,last} × mode=block` 全笛卡尔积，即 13 cells/model、39 cells total；即使
Gate D 终态为合法 `ABSTAIN`，也直接采用 width-4 finite score top-3，不因 eligibility 缩减 panel。

该扩展由 `.planning/phase7/loopscope_phase7_gate_e_outcome_amendment_20260815.md` 冻结。当前
Gate B executor、handoff、权限和 terminal condition 完全不变；本次只改变未来 phase route。
Gate C/D 仍按原 outcome-blind 路线执行。Gate D `PASS` 后，planning 必须先完成原定绘图、三格式
QA、outcome-blind 报告和 exact top-3 panel freeze，才 admission Gate E。Gate E 必须使用新的独立
executor，预注册标题为 `execute-LoopScope-Top3-Outcome-第7阶段-Gate E`；在 exact
handoff 前不得读取 test/outcome、运行 loop、创建 job 或 heartbeat。

Gate E formal submission 必须先通过同 immutable decision-critical path 的 HPC2 `debug` `<30min`
preflight；正式资源按 normal user 合法最高优先级和显存/runtime/queue/time-to-result 选择，正式 job
ID 核实后使用唯一 60-minute full heartbeat。39/39 cells 完整前不得做 partial outcome-driven panel
change。Gate E planning `PASS` 后由 planning 更新报告并做 consolidated phase audit，Phase 7 才
terminal；Gate E 后不自动创建 Gate F。

## 17. Gate B 终审 PASS 与 Gate C 授权

Planning 于 2026-08-15 收到 exact Gate B executor
`01a001c1-3ff9-7370-abbb-b32128897a08` 的 `GATE_B_FINAL_AUDIT`，并执行协议规定的轻量终审。
Local 与 HPC2 dedicated checkout 均闭合于 branch `loopscope`、commit
`a95772a6bded50ae62e4ffcbb8b0b24c374b7b20`；远端 worktree clean，本地 staged membership 为空，
七个 protected tracked dirty paths 未被触碰。Planning 直接读取三个 retry-3 receipt，确认每模型均为
`PASS`、4 records / 4 unique identities / 2 subjects / 4 native forwards / zero loop；独立读取 Slurm
accounting，确认 preflight jobs `10217716/17/18` 与 smoke jobs `10217722/23/24` 全部
`COMPLETED 0:0`，单 job 14–20 秒。

前两次失败分别暴露 inference-mode boundary 与 shape-sensitive BF16 native-logit closure 问题；两次
focused repair 均已 commit、push、remote fast-forward，并由新的三模型 CUDA retry 与 fresh verifier
闭合，因此失败 roots 保留但不进入最终 evidence。完整决定见
`.planning/phase7/loopscope_phase7_gate_b_audit_decision_pass_20260815.md`。

Planning 因此判定 Gate B `PASS` 并撤销其 executor 权限。依 standing continuation，Gate C 已绑定
新的独立 executor `01a00215-a3a1-7841-9079-e87cf78571e0`，标题后经用户要求整改为
`execute-LoopScope-Full-Acquisition-第7阶段-Gate C`，当时的唯一授权由
`.planning/phase7/loopscope_phase7_gate_c_handoff.md` 冻结。Gate C 只采集、merge、验证并封存三个
模型共同 MMLU validation-1531/57 的 sanitized trajectory；不得运行 V3、绘图、loop/generation、
test 或 outcome。Gate D、planning plotting checkpoint、Gate E 与 planning closeout 继续锁定。

## 18. Gate B/C task 命名整改与 Gate C executor recovery

用户于 2026-08-15 指出 Gate B/C executor task 标题不符合 `research-gate-orchestrator` 的
Gate-local topic 规则，并报告原 Gate C executor 异常终止。Planning 直接读取两个任务状态后确认：
Gate B 已正常终态；原 Gate C executor 已完成并推送 Gate C commit
`feb8a9f67546db22658d2d62053d8b69b1c0b286`，三模型 debug preflight jobs
`10217835/10217836/10217837` 均 `COMPLETED 0:0`，但正式 shard jobs 尚未提交。该 executor 在正式
提交被审批器拦截后直接向用户请求批准，没有向 planning 投递 `GATE_C_FINAL_AUDIT`、真正的
`BLOCK` 或 `TERMINAL_DELIVERY_UNCONFIRMED`，也没有可见交付确认。这是严重的 terminal-delivery
与 continuation-routing 协议违规，不是科学 Gate 失败。

Planning 已将 Gate B task `01a001c1-3ff9-7370-abbb-b32128897a08` 重命名为
`execute-LoopScope-Debug-Smoke-第7阶段-Gate B`；将旧 Gate C task
`01a00215-a3a1-7841-9079-e87cf78571e0` 重命名为
`execute-LoopScope-Full-Acquisition-第7阶段-Gate C` 后撤权并保留为可见只读 provenance。完整 incident 与保留证据见
`.planning/phase7/loopscope_phase7_gate_c_executor_recovery_decision_20260815.md`。

Gate C 科学合同保持不变，现绑定 replacement executor
`01a0036f-fc1d-7412-b043-b93b5f088398`，标题
`execute-LoopScope-Full-Acquisition-第7阶段-Gate C`。replacement iteration 只由 exact thread ID
和 recovery handoff 文件名区分，不进入 Gate 字段。该任务不显式覆盖 model 或 reasoning，继承
Codex config 默认配置；唯一授权由
`.planning/phase7/loopscope_phase7_gate_c_recovery_handoff_2.md` 冻结。新 executor 从已提交、已
preflight 的 `feb8a9f...` 继续，不重跑无漂移的通过证据；formal job IDs 生成后使用唯一 60-minute
full heartbeat。每次 handoff 必须明确要求完整遵守 `research-gate-orchestrator` 的协作、subagent、
heartbeat、单一终态事件及可见投递确认规则。后续 canonical titles 为 Gate D
`execute-LoopScope-V3-Scoring-第7阶段-Gate D` 与 Gate E
`execute-LoopScope-Top3-Outcome-第7阶段-Gate E`。Gate D 及以后继续锁定。

## 19. Gate C 终审 PASS 与 Gate D 待绑定

Planning 于 2026-08-15 收到 replacement executor
`01a0036f-fc1d-7412-b043-b93b5f088398` 的 `GATE_C_FINAL_AUDIT`，并按
`research-gate-orchestrator` 完成材料性轻量验收。Local、`origin/loopscope` 和 HPC2 dedicated
checkout 均闭合于 commit `bf37259a71ef4220236f51e5b659141748653f0b`；远端 clean、本地 staged
为空，七个 protected dirty paths 未被触碰。Slurm jobs `10222432/10222433` 均
`COMPLETED 0:0`；三模型在 packed A800 路径上各自产生两个 `[765,766]` shards，六个 child exits
均为 0，无 OOM 或 network violation。

Planning 直接读取三份 sealed fresh verifier receipts，确认 Qwen2.5-3B、Llama-3.2-3B、
Gemma-2-2B 分别以 `L=36/28/26` 闭合共同的 1,531 identities / 57 subjects、zero loop、exact
membership、`L+1/L` shapes、FinalNorm/logit closure、有限六类 scalar arrays 与 sanitized
information barrier。Gate 内的 offline/cache/progress 修复已测试、已 commit 且 science 不变；早期失败
roots 被保留但未进入 sealed evidence。因此 Gate C 最终判定为 `PASS`，完整决定见
`.planning/phase7/loopscope_phase7_gate_c_audit_decision_pass_20260815.md`，executor 权限已经撤销。

Standing continuation 已创建并绑定新的独立 Gate D executor
`01a00522-11a9-70d1-80e4-5d4401deb68c`，canonical title 为
`execute-LoopScope-V3-Scoring-第7阶段-Gate D`；任务未显式覆盖 model/reasoning，继承 Codex config
默认配置。Exact task ID 已写入本 control 与
`.planning/phase7/loopscope_phase7_gate_d_handoff.md`，Gate D 正式 `AUTHORIZED`。Gate D 只读
Gate C sealed records，CPU-only 运行一次 frozen V3.1 scoring 与 fresh verifier；不使用 GPU/Slurm、
不绘图、不访问 test/outcome，也没有 Gate E 权限。Planning plotting checkpoint 与 Gate E 继续锁定。

## 20. Gate D 终审 PASS 与 planning plotting checkpoint

Planning 于 2026-08-15 收到 Gate D executor
`01a00522-11a9-70d1-80e4-5d4401deb68c` 的 `GATE_D_FINAL_AUDIT`。最小独立验收确认 local、
`origin/loopscope` 与 HPC2 dedicated checkout 均为 commit
`6547f68715421a0725a3816b397b814717c10653`，远端 clean、本地 staged 为空，protected dirty
paths 保持原样。Planning 直接读取 final Gate D manifest、三份 analysis 与三份 fresh verifier
receipts：三模型均为 `PASS` 和 `independent_recomputation=true`，1,531 records / 57 subjects 与
`42/26/26` candidate domains 完整闭合。

Qwen2.5-3B 与 Llama-3.2-3B 分别得到 `SELECTED_WINDOW 14:17`（frequency 0.973）和
`SELECTED_WINDOW 11:14`（frequency 1.000）；Gemma-2-2B 得到合法
`ABSTAIN_COMBINED_RANK_UNSTABLE`，point winner `[3,13]`、frequency 0.467。每模型 final analysis
均封存五类 boundary metrics、adjacent angular transitions、95% bootstrap CIs、完整 candidate table
和 selector summary，且 diagnostics 与 selector projection 分离。完整决定见
`.planning/phase7/loopscope_phase7_gate_d_audit_decision_pass_20260815.md`。

Gate D 判定 `PASS`，其 executor 权限撤销。Planning plotting checkpoint 现为唯一活动工作：只读
Gate D aggregate/source-data，使用 Python 生成逐层轨迹、V3 score landscape 与 normalized-depth
跨模型对照，完成 SVG/PDF/PNG、source-data 和视觉 QA，并在任何 test/outcome 访问前冻结每模型
width-4 finite score top-3 panel。该 checkpoint 完成并写入 PASS 决定前不得创建 Gate E executor。

## 21. Planning plotting PASS 与 Gate E panel blocker

Planning 于 2026-08-15 亲自完成 outcome-blind plotting checkpoint。只读 Gate D final aggregate
source-data 后，用 Python/Matplotlib 生成每模型 6 张 native-depth trajectories、每模型 1 张 V3 score
landscape 和 6 张 normalized-depth cross-model comparisons，共 27 张 canonical figures；v2 以 SVG、
PDF、600-dpi PNG 输出 81 个文件。自动 source/export checks 与四张 contact-sheet 视觉 QA 均 PASS，
中文 outcome-blind 报告已写入 `资产/报告/phase7/`。v1 半开窗口标签歧义只在 fresh v2 presentation
sibling 修复，没有触碰 Gate D science 或 sealed artifacts。

Panel-freeze 检查在任何 test/outcome 访问前执行。按冻结规则过滤 width=4 并按 finite
`S_RATE_TURN` 排序后，Qwen/Llama/Gemma 的有限候选数分别为 `4/3/2`；Gemma 只有 `[10,14)` 和
`[12,16)` 两个，无法形成每模型 top-3。Gate E amendment 明确规定不足三个时
`BLOCK_PANEL_NOT_FORMABLE` 且禁止 fallback，所以 plotting/QA 为 PASS，但 Gate E admission 为
BLOCK。完整 checkpoint 决定见
`.planning/phase7/loopscope_phase7_plotting_checkpoint_decision_20260815.md`；pre-outcome panel manifest
见 `资产/报告/phase7/phase7_gate_e_panel_manifest.json`。

当前没有 authorized executor，未创建 Gate E task，未读取 MMLU test/outcome，也未运行 preflight、
GPU/Slurm/job/heartbeat。继续 Gate E 需要用户明确批准新的科学 amendment，或明确终止 Gate E；planning
不得自行选择 fallback。

## 22. 用户批准 3/3/2 panel 与 Gate E 授权

用户于 2026-08-15 在未读取任何 test/outcome 的状态下明确批准 planning 建议的最小
科学修订：Qwen/Llama 保留 finite width-4 `S_RATE_TURN` top-3，Gemma 使用现有
top-2。修订记录见 `.planning/phase7/loopscope_phase7_gate_e_332_panel_amendment_20260815.md`，
canonical pre-outcome panel 见 `资产/报告/phase7/phase7_gate_e_panel_manifest.json`。窗口分别为：

```text
Qwen2.5-3B: [14,18), [19,23), [16,20) -> 13 cells
Llama-3.2-3B: [10,14), [11,15), [9,13) -> 13 cells
Gemma-2-2B: [10,14), [12,16) -> 9 cells
total=35 cells
```

Planning 已创建独立 executor `01a00569-e66d-7eb2-9499-56b01d268366`，并核对 canonical title
`execute-LoopScope-TopAvailable-Outcome-第7阶段-Gate E`。该 task 不显式覆盖 model/reasoning，
继承 Codex config 默认配置。唯一授权由
`.planning/phase7/loopscope_phase7_gate_e_handoff.md` 冻结。Gate E 先实现/验证窄的 35-cell
outcome 路径和 pre-outcome completeness barrier，再用 HPC2 `debug` partition 执行 `<30min`
end-to-end preflight；只有 preflight PASS 才能提交 formal jobs。正式 job IDs 核实后使用唯一
60-minute full heartbeat，terminal 时必须唤醒 exact executor。

35/35 cells 及同一 test-14042 membership 闭合前不得读取或报告部分 accuracy/gain；
完整后只运行一次 combined analysis 与 fresh verifier。Executor 仅能向 planning thread
`01a0013e-71c3-7c90-a547-4059b462dc7e` 发送唯一 `GATE_E_FINAL_AUDIT` 或真正的
`BLOCK`；不得进入 planning closeout、更新最终图表/报告或创建 Gate F。

## 23. Gate E 正式资源利用原则补充

用户进一步明确：正式全量 outcome 的资源宗旨是在不 OOM 的硬边界内最大化有效显存利用和总体
吞吐。Supplement 1 已作为历史授权保留；Gate E 当前资源恢复与 formal 扩展的 canonical authority
更新为 `.planning/phase7/loopscope_phase7_gate_e_resource_recovery_supplement_2.md`。该补充把
functional-path preflight 与 formal resource-shape evidence 分开，并要求先以 panel 内真实 cell
完成可保留 formal canary，再扩展剩余任务。`batch_size=16`、全部科学配置、35-cell panel 与
outcome barrier 保持不变。

## 24. Gate E 资源恢复与 formal canary admission

Gate E 当前 decision 为 `GATE_E_RESOURCE_RECOVERY_AUTHORIZED_FORMAL_CANARY_REQUIRED`，下一次
formal 扩展 admission 为 `VALID_RETAINABLE_CANARY_PASS`。Tiny/underfilled debug 不得再用于估算
formal packing；显存证据必须按模型与资源行为不同的 cell family 闭合。计算节点 launcher 不得依赖
Git executable、Modules 安装、登录节点 PATH 或隐式 shell。已有 `10226946[0-7]` 按 active
supplement 的条件过渡规则处理：保留任何 valid cells，用户取消与异常失败分开记录，不覆盖旧 root。

## 25. Gate E Gemma batch-size runtime recovery

Gate E canary 已证明 Gemma cell 26/34 在独占单 A800、真实 `batch_size=16` 下 OOM。用户随后明确
允许降低 batch size；因此尚未投递执行的 two-A800 proposal 由
`.planning/phase7/loopscope_phase7_gate_e_gemma_batch_recovery_supplement_4.md` 取代。Qwen/Llama
保持 batch 16；Gemma 从单 A800 batch 8 开始，以 `8 -> 4 -> 2 -> 1` 的有界 ladder 冻结最高通过值，
不改变模型、membership、panel 或结果语义。当前 array/task 15 和 heartbeat 先自然终态并保留；之后
闭合 Gemma functional debug、cell 34、cell 26 与联合五-cell canary。只有
`FIVE_CELL_CANARY_PASS_WITH_FROZEN_GEMMA_BATCH` 后才能扩展 remaining missing cells。Broad
expansion、partial outcome 与 planning closeout 在此之前继续锁定。

## 26. Gate E PASS 与 Phase 7 终态

Planning 于 2026-08-16 收到 exact executor
`01a00569-e66d-7eb2-9499-56b01d268366` 的唯一 `GATE_E_FINAL_AUDIT`。轻量验收直接确认：

- local、`origin/loopscope` 与 HPC2 dedicated checkout 均闭合于 commit
  `65b39762caed6ab2443d03314d29c58d73d20f3d`；远端 clean、本地 staged 为空，七个 protected
  tracked dirty paths 未被触碰。
- Formal array `10232717` 的 24 个 missing-cell tasks 全部 `COMPLETED 0:0`。最终
  pre-outcome verifier 为 `PASS`：35/35 cells、Qwen/Llama/Gemma 分别 13/13/9，每 cell
  14,042 identities / 57 subjects，无 missing、duplicate、extra 或 partial，且 barrier 前
  `outcome_aggregates_computed=false`。
- 完整性闭合后仅执行一次 combined analysis，2,000 次 subject-stratified paired bootstrap，
  seed `20260803`；fresh verifier 为 `PASS`且 `independent_recomputation=true`。

Gate E 因此判定 `PASS`，executor 权限撤销，不创建 Gate F。第七阶段 Gate A–E、
planning-owned 绘图/QA、最终 outcome 报告与 consolidated audit 全部完成，阶段终态为
`PHASE7_TERMINAL_PASS`。科学结论为：Qwen/Llama 的最佳观察 loop cell 仅有极小正点估计且
95% CI 均跨 0；Gemma 全部 loop cells 点估计均为负。本阶段不声称存在可确证的
training-free loop MMLU 收益。完整决定与阶段终审见
`.planning/phase7/loopscope_phase7_gate_e_and_phase_audit_pass_20260816.md`。

## 27. 用户追加 Gate F V3 point-rank Top-2 outcome

用户于 2026-08-16 在 Gate E 终态与结果表已完成后，明确要求三模型各自 V3.1
模型内全 width point-rank 前二窗口追加全量 loop outcome，继续遍历 `k={2,3}`、
`cache={first,last}`、`mode=block`。该指令显式重开 Phase 7 并取代原“不创建 Gate F”
的停止分支，但不改变 Gate A–E 已验收 evidence 或 Gate E 原结论。

Gate F 冻结窗口为 Qwen `[14,17)`/`[12,15)`、Llama `[11,14)`/`[10,13)`、Gemma
`[13,16)`/`[10,13)`；每模型 8 个新 loop cells，合计 24。复用 Gate E 三个 exact
no-loop baselines，不重跑；分析 panel 为 27 cells。决策在已知 Gate E outcome 后追加，
所有结果只能标为 `POST_TERMINAL_V3_POINT_RANK_TOP2_OUTCOME_EXPLORATORY`。

Planning 已创建并绑定新的独立 executor
`01a00965-fa7f-7411-b060-0c8315a94711`，title 为
`execute-LoopScope-V3-Top2-Outcome-第7阶段-Gate F`，不显式覆盖 model/reasoning，继承用户
Codex config 默认值。唯一授权由 `.planning/phase7/loopscope_phase7_gate_f_handoff.md`
冻结。Gate G 和 planning recclose 继续锁定。

Gate F 最终由 planning 判定 `PASS`：commit `08da7733a1e8402677184c860d0ec35f041335aa`；
formal array `10239586` 的 24 个任务全部 `COMPLETED 0:0`；pre-outcome verifier 确认 27 cells、
24 execution cells、3 retained baselines，每 cell 14,042 identities / 57 subjects；combined analysis
完成且 fresh verifier `PASS`、`independent_recomputation=true`。所有结果保持
`POST_TERMINAL_V3_POINT_RANK_TOP2_OUTCOME_EXPLORATORY` 与 nominal non-confirmatory 边界。
Gate F executor 已撤权，planning recclose 完成，Phase 7 重新终态；不创建 Gate G。
