# Gate G：用户授权全量提交与增大批量

用户最新明确“直接提交全量实验吧，bs开得大一些吃满显存”。本补充覆盖原G等待用户再次通知canary完成后才提交余量的顺序，以及固定batch1限制；其他科学配置、4basis、9cell、完整14042身份、gold屏障和24GPUh/同时2GPU不变。executor仍01a08443-1e4f-7623-aa1b-69d3dbae406e，planning仍01a08024-3556-75c1-8209-64e89345b940。所有timer保持关闭。

立即核实既有canaries12696882/12696883，保留有效0:512，不重复校准或有效评分。无需等用户再次批准全量。先以最短实现检查确定当前真实batch路径：增加CLI batch_size若底层仍逐prompt调用不是增加实际batch，不得宣称已扩大。允许在G专属/phase8辅助评分与干预模块最小实现批处理，保护原默认路径；wrapper/strategies/cache/config仍不直接改，若真实必要给planning最小复现和diff。禁止只处理batch第0个样本或共享错误pre-answer位置；每条prompt和选项的身份/长度/位置/cache必须正确对应。

选择实际batch候选2/4/8，按代表性最长输入/K4和三种arm实测，以吞吐及峰值决定，不必逐个穷举，保留至少15%瞬态余量，不为显示100%占用冒OOM风险。无需新增全套测试。新增batch运行路径必须在debug partition以同immutable源码/launcher/env通过<30min端到端检查后正式。用同一无gold样本与batch1对比每题四个分数、排序/预测、干预位置及步次，记录最大数值偏差；出现预测改变或非舍入级分数漂移，不把两种batch输出无说明混合，先定位工程原因或向planning报告科学可比性问题，不读gold挑batch。上尾内存不足降低batch，不删长题/截断改变输入。若当前batch1约束无法小改且安全保持语义，优先使用原实现多独立进程packing2–3作为显存利用方案，明确这是并发不是bs；不得为增batch无限延误全量。

同批处理资源形状不能由小样本debug代表时，用最小可保留正式区间canary验证后扩展；用户免除再次等待授权，不免除基本正确性。原0:512可在分数/行为对照成立后与新批量余量拼接，记录batch分片 provenance；所有arms采用相同分片和数值设置。不要静默以新batch重跑已完成结果。

全量余量512:14042可一次提交所有必要jobs，使用依赖关系保证canary成功和同时<=2GPU，无重复范围；若新batch验证尚未通过，不能提前让新路径全量运行。正式仍最高合法A800，job<=6h，总24GPUh。记录真实batch、packing、GPU峰值/吞吐/OOM及源码/区间。若batch无法合格，使用已验证原路径并发完成全量，不改变模型dtype或lambda以填显存。

执行者自主完成本次短验证/全量提交，不创建heartbeat/后台轮询。若需等待新作业，报告job IDs和明确手动续接点；用户manual-only仍覆盖所有自动监控条款。全9cell闭合后一次分析，原三类各3项Holm不变。所有新工件write-once；低风险修复仅缺失/无效工作。
