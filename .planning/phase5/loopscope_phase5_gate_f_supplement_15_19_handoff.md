# GATE_F_SUPPLEMENT_15_19_HANDOFF

```text
Project/phase: LoopScope Phase 5 post-terminal extension
Planning/audit thread: 019f8604-4717-7be2-8bf8-9d4a26a3d7f7
Authorized executor thread: 019f8eb5-2f08-7c80-afc2-b4ae52464162
Executor title: execute-LoopScope-MultiWidth-Top4Outcome-第5阶段-Gate F
Gate: F — supplement one fifth cell 15:19 and analyze baseline + five
Supersedes: only the four-cell membership/analysis prohibition in the original Gate F handoff
Preserves: every other original Gate F recipe, information barrier, protected-state and Git rule
```

## Planning disposition

The executor's `BLOCK` is resolved by the user's explicit scientific-scope authorization to
“追加一组15:19”. Gate F remains open under the same independent executor. This is a bounded
same-Gate scope extension, not a new Gate, repair of observed outcomes, or permission for any
additional window.

## Frozen scientific matrix

Preserve the original four cells exactly as launched:

| source | Gate E rank | width | window | score |
|---|---:|---:|:---:|---:|
| original formal `10048669` | 1 | 5 | `13:17` | 23.122176074886756 |
| original formal `10048669` | 2 | 6 | `15:20` | 20.459460381229054 |
| original formal `10048669` | 3 | 6 | `12:17` | 16.292884498978600 |
| original formal `10048669` | 4 | 3 | `15:17` | 11.683278605031116 |

Add exactly one cell:

| source | Gate E rank | Width rank | width | window | score |
|---|---:|---:|---:|:---:|---:|
| supplemental formal | 11 | 2 | 5 | `15:19` | 3.0677387 |

The supplemental implementation must derive and verify `15:19` from the exact Gate E score JSON,
not only from this text. No sixth cell, reranking, threshold/selector change, baseline rerun,
`15:18` rerun, K sweep, or other scientific expansion is authorized.

## Preserved original four-cell execution

- Original immutable root:
  `/hpc2hdd/home/xhuang225/workspaces/training_free_looped_transformers_loopscope/runs/phase5-gate-f-multiwidth-top4-outcome-20260723T114440Z`
- Original formal array: `10048669`, indices `0..3`.
- Do not cancel, requeue, change membership, alter staged code/manifests, overwrite, append scientific
  records, or reuse this root for `15:19`.
- Keep the existing executor-owned monitor read-only. On terminal success, stop/pause it and run the
  original four-cell structural sealer without parsing outcome values.
- If any original task fails, apply only the retry rule already frozen in the original handoff.
  Otherwise report `BLOCK`; this supplement does not broaden formal retry authority.
- Do not run the original four-cell unseal/analysis after its structural seal. Its four cells remain
  sealed until the supplemental cell also closes.

## Supplemental 15:19 execution

Use one new write-once sibling root with prefix:

`/hpc2hdd/home/xhuang225/workspaces/training_free_looped_transformers_loopscope/runs/phase5-gate-f-supplement-15-19-outcome-`

The cell must use the unchanged earlier full-outcome recipe:

```text
model=Qwen/Qwen3-4B-Base@906bfd4b4dc7f14ee4320094d8b41684abff8539
dataset=cais/mmlu@c30699e8356da336a370243923dbaf21066bb9fe
task=mmlu
split=test
num_fewshot=5
population=14042 identities / 57 subjects
ordered_identity_sha256=2c4557079fca1a7c071460f779ed714805fc96c3391560825cd34386d4051532
lm_eval=0.4.11
metric=acc,none
width=5
window=15:19
k=3
operator_body_calls_per_prefill=3
strategy=euler / damped_euler_alias
alpha=1.0
step_size=1/3
total_horizon=1
iteration_mode=block
cache_strategy=first
decode_mode=bypass
dtype=bfloat16
batch_size=16
log_samples=true
```

After the original four cells have structurally sealed outcome-blind:

1. Make the smallest card/adapter/launcher/test update needed to bind the fifth cell and the new
   combined matrix. Preserve the original root's staged implementation hashes as immutable evidence.
2. Run targeted tests plus one remote CPU dry-run proving exact Gate E rank-11 membership, unchanged
   recipe, separate roots, and baseline+5 analysis shape.
3. Width-5 runtime semantics already passed the original Gate F smoke through `13:17`. A new GPU
   smoke is optional, not mandatory. Run at most one single-cell limit smoke only if the changed
   launcher path cannot be closed by the targeted test/dry-run.
4. Submit exactly one full `15:19` Slurm task on the same A800-equivalent resource class. No array
   membership beyond this one cell and no automatic retry/requeue.
5. Install at most one low-frequency executor-owned outcome-blind monitor for this supplemental job.
6. On terminal success, structurally seal exactly 14,042 identities / 57 subjects, with
   missing/duplicate/extra all zero, without parsing scientific values.

One executor-owned low-risk implementation/smoke repair is allowed before the supplemental formal
launch. After supplemental formal launch, no code/science/candidate/recipe mutation is allowed.
A failed formal task may be retried once only when a write-once failure receipt mechanically proves
model/evaluator forward requests=0 and scientific records=0; otherwise report `BLOCK`.

## Combined one-shot analysis

Only after both structural closures exist:

1. Bind the canonical Gate C baseline artifact by original file identity/hash.
2. Bind all four original sealed result artifacts and the sealed `15:19` artifact by identity/hash.
3. Create one new write-once CPU-only aggregation root with prefix
   `phase5-gate-f-combined-top4-plus-15-19-`.
4. Perform exactly one combined unseal/analysis and one independent verifier pass. Do not first
   analyze the four-cell subset.
5. Recompute baseline correctness from its canonical raw artifact. Use a 57-subject-stratified joint
   paired bootstrap with R=2000, seed=20260723, shared draw stream across baseline plus all five new
   cells, SE ddof=1 and the existing linear percentile definition.
6. Report for each of the five windows: Gate E rank/score/width/window, accuracy, gain versus
   baseline, paired 95% CI, four transition counts and exact two-sided McNemar p.
7. Existing `15:18` may appear only as a clearly separated read-only reference. It is not part of
   the five-cell joint bootstrap.

The information barrier remains active until both structural seals close. Before then, do not open,
parse, compute, copy or report `results.json`, logged samples, metrics, predictions, correctness,
accuracy, gain, flip or any partial window performance. No outcome-informed repair is permitted.

## Allowed files and Git

The executor may minimally update the four existing Gate F paths:

- `configs/loopscope/phase5_multiwidth_top4_outcome_card.json`
- `src/tflt/loopscope/phase5_variable_width_outcome.py`
- `scripts/loopscope/run_qwen4base_phase5_gate_f.py`
- `tests/test_loopscope_phase5_gate_f.py`

It may add one supplement card if cleaner:

- `configs/loopscope/phase5_gate_f_supplement_15_19_card.json`

Final report remains:

- `报告/LoopScope_第五阶段多宽度Top4全量Outcome.md`

Create at most one additional local-only commit for this scope extension. No push is authorized.
All original Gate C/D/E artifacts and protected runtime paths remain read-only. No rebase,
force-push, merge-main, destructive cleanup, overwrite or deletion.

## Minimum decisive checks and stop rule

Executor verification must establish only:

- original job/root preserved and four cells structurally sealed;
- exact Gate E-derived `15:19`, unchanged K=3 recipe and one supplemental formal job;
- exact fifth-cell 14,042/57 identity closure;
- exactly one baseline+5 analysis and one verifier;
- five-row metrics table plus information-barrier and protected-state closure.

Planning acceptance will inspect:

1. original four-task and supplemental one-task scheduler/identity closure;
2. one raw-sample recomputation against the canonical baseline;
3. combined verifier plus one-shot receipt.

Stop after the requested five accuracies and paired statistics close. Do not add figures, further
windows, extra reruns, generalized framework work or a next Gate.

## Terminal requirement

After resumption, send exactly one new terminal event to planning thread
`019f8604-4717-7be2-8bf8-9d4a26a3d7f7`:

- `GATE_F_FINAL_AUDIT` when all five rows and the combined verifier close; or
- `BLOCK` if either sealed acquisition or the combined one-shot analysis cannot legally complete.

The final packet must distinguish the original four-cell root/job from the supplemental
`15:19` root/job, report both structural seals, and provide the combined baseline+5 table. Gate F
does not authorize any later Gate.
